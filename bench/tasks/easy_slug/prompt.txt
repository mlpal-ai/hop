Bug report: titles with accented characters lose those letters entirely — slugify("Café déjà vu") currently returns "caf-d-j-vu" but per the docstring it must transliterate accented latin letters to their ASCII base: "cafe-deja-vu". Characters with no ASCII base (e.g. CJK) should still be dropped. Fix slugify() using only the Python standard library, keep signatures, and make sure the empty-result fallback in unique_slug still works.
