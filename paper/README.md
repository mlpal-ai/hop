# HOP paper

`hop-paper.tex` + `fig/` build with tectonic: `tectonic hop-paper.tex`.
`hop-paper.pdf` is the compiled paper. Figures regenerate via `python3 make_figs.py`
(matplotlib). Evidence: `../bench/` (H1 parity + H2 reviewer campaigns, raw logs).
